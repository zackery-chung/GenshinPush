# Genshin Push

## How To Use

### Complete the config

copy `config.json.example` to `config.json` and complete the config field.

### Install Dependences
```
  npm install
```

### Start Push
```
  npm run start
```